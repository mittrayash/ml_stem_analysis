
# This is appended to the file.
def test_first():
    assert first([1, 2, 3]) == 1
# This is appended to the file.
def test_first():
    assert first([1, 2, 3]) == 1
# This is appended to the file.
def test_first():
    assert first([1, 2, 3]) == 1
# This is appended to the file.
def test_first():
    assert first([1, 2, 3]) == 1